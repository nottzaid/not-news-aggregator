[package]
name = "not-news-app"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
not-news-domain = { path = "../domain" }
not-news-platform = { path = "../platform" }
not-news-renderer = { path = "../renderer" }
not-news-store = { path = "../store" }

[dev-dependencies]
indexmap.workspace = true

[lints]
workspace = true
