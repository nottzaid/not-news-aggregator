class ResearchSessionClient {
  const ResearchSessionClient();

  Future<String> transcribeRecording(String path) {
    throw UnsupportedError(
        'Audio upload is not available on this platform yet.');
  }
}
