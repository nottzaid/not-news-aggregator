web:
  search_backend: exa
  extract_backend: exa

memory:
  memory_enabled: true
  user_profile_enabled: true

skills:
  external_dirs: []

terminal:
  env_passthrough:
    - AI_NEWS_SEARXNG_URL
    - AI_NEWS_SEARXNG_SEARCH_URL
    - BROWSE_CLI
